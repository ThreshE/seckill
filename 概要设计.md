我非常建议你采用这个方式学习 Java 并发。

因为你现在的问题不是“不知道 synchronized 是什么”，而是：

不知道这些并发技术为什么在真实系统里出现，以及它们解决什么生产问题。

你上传的 Java 知识体系图里，把 Java并发列为 P0 必须掌握，并标注当前掌握度约 30%，重点补充。
同时你的目标岗位是中级 Java 工程师，这类岗位面试不会只问：

synchronized 是什么？

而会问：

“双十一秒杀库存扣减，你如何保证不超卖？”

“Redis库存和MySQL库存如何保证一致？”

“50万QPS流量进来，线程池怎么设计？”

“为什么不用 synchronized 锁整个方法？”

“Redis分布式锁失效怎么办？”

所以你的这个项目方向非常适合作为：

Java并发 + Redis + MySQL + Spring Cloud + 分布式系统综合学习项目

我会按照真实企业项目设计。

不过由于完整设计文档会比较长（预计 50~80 页 Markdown），一次回复放不下。

我会拆成几个阶段输出：

《双十一秒杀系统设计文档》

版本：

Seckill System V1.0

技术栈：

Spring Boot 3
Spring Cloud Alibaba
Nacos
OpenFeign
Gateway
MySQL 8
Redis 7
Docker Desktop
Maven
ApiFox
JDK17
第一部分：项目目标
1.1 项目背景

模拟淘宝/京东双十一秒杀：

商品：

iPhone 17 Pro

库存：
1000

价格：
7999元

活动开始：

10:00:00

瞬间：

500000 QPS请求

目标：

支持：

大量用户同时抢购

保证：

1. 不超卖

2. 不重复购买

3. 库存最终一致

4. 支付金额正确

5. 服务稳定
第二部分：整体架构设计

整体：

                 用户

                  |
                  |

             ApiFox压力测试

                  |

              Spring Cloud Gateway

                  |

        -------------------------

        |                       |

 秒杀服务                 用户服务


        |

        |

 Redis库存层

        |

        |

 MySQL订单/库存


        |

        |

 MQ异步削峰(可选)


第三部分：微服务拆分
1. seckill-gateway

职责：

统一入口

功能：

路由
限流
黑名单
Token校验

技术：

Spring Cloud Gateway

2. seckill-service

核心服务。

负责：

创建秒杀订单

扣库存

扣金额

接口：

POST

/api/seckill/buy

请求：

{
 "userId":10001,
 "goodsId":1
}


返回：

成功：

{
 "code":200,
 "msg":"抢购成功",
 "orderId":"10000001"
}


失败：

{
"code":500,
"msg":"库存不足"
}

3. stock-service

库存服务。

负责：

库存初始化

库存扣减

库存恢复
4. order-service

订单服务。

负责：

生成订单：

订单号

用户

商品

金额

状态
5. payment-service

模拟支付。

这里只做：

扣减余额
第四部分：Docker部署

docker-compose:

目录：

seckill-system

│

├── docker-compose.yml

│

├── mysql

│

├── redis

│

├── nacos

│

└── seata

启动：

docker compose up -d
第五部分：数据库设计
商品库存表

goods_stock

CREATE TABLE goods_stock(

 id bigint PRIMARY KEY,

 goods_name varchar(100),

 stock int,

 price decimal(10,2),

 version int

);


例如：

字段	值
id	1
goods	iphone
stock	1000
price	7999
version	0
秒杀订单表
CREATE TABLE seckill_order(

id bigint,

user_id bigint,

goods_id bigint,

amount decimal,

status varchar(20),

create_time datetime

)

并发日志表

这个是为了学习。

CREATE TABLE concurrency_log(

id bigint,

trace_id varchar(64),

thread_name varchar(100),

step varchar(100),

before_stock int,

after_stock int,

lock_type varchar(50),

cost_time bigint,

create_time datetime

)


例如：

记录：

线程：

http-nio-8080-exec-12


执行:

Redis扣库存


库存:

1000->999


耗时:

3ms


使用:

Redis Lua


第六部分：秒杀完整流程
第一阶段：请求进入

用户：

ApiFox

50000并发

进入：

Gateway

日志：

[Gateway]

requestId:
abc123

thread:
nio-worker-1

time:
10:00:00.001


学习点：

对应Java知识：

线程池

为什么？

因为：

Tomcat本身也是线程池。

第二阶段：限流

问题：

500000 QPS

服务直接挂。

方案：

令牌桶。

例如：

允许：

5000 QPS

超过：

直接失败。

学习：

线程池
阻塞队列
拒绝策略
第三阶段：Redis预扣库存

启动活动前：

MySQL:

stock=1000

加载：

Redis:

seckill:goods:1

value:

1000


用户请求：

执行：

Lua脚本

伪代码：

local stock =
redis.call('get',KEYS[1])


if stock<=0 then

 return -1

end


redis.call(
'decr',
KEYS[1]
)


return 1


为什么Lua？

因为：

Redis单线程。

整个脚本：

原子执行。

对应：

Java并发：

原子性

第七部分：防止超卖方案比较
方案1 synchronized

代码：

synchronized(stock){

 stock--;

}


优点：

简单。

缺点：

分布式失效。

为什么？

两个服务：

服务A JVM

锁


服务B JVM

不知道

所以：

只能单机。

对应学习：

synchronized。

方案2 Redis分布式锁

例如：

Redisson

lock.lock()


扣库存


unlock()


优点：

分布式。

缺点：

性能一般。

问题：

锁失效。

比如：

线程拿锁：

30秒

业务：

40秒

锁自动释放。

其他线程进入。

方案3 Redis Lua（推荐）

直接：

判断库存

+
扣库存


一次完成。

优势：

性能最高。

本项目采用。

第八部分：这里开始重点学习你的薄弱点

这个项目会刻意设计几个版本。

Version 1

错误版本：

直接MySQL扣库存

你会看到：

500线程：

结果：

库存：

-50

学习：

为什么需要锁。

涉及：

synchronized
数据库锁
事务
Version 2

加入：

synchronized

结果：

单机正常。

但是启动：

两个服务：

继续超卖。

学习：

为什么需要AQS。

Version 3

加入：

Redis Lock

学习：

AQS思想

分布式锁

CAS

Version 4

加入：

Redis Lua

学习：

原子操作

最终一致
Version 5

加入：

线程池 + MQ

学习：

削峰填谷

异步处理

第九部分：线程池设计

秒杀服务：

配置：

@Bean
public ThreadPoolExecutor seckillExecutor(){


return new ThreadPoolExecutor(

20,

100,

60,

TimeUnit.SECONDS,

new ArrayBlockingQueue<>(1000),

new ThreadFactory(),

new AbortPolicy()

);

}


解释：

核心线程：

20

最大：

100

队列：

1000

超过：

拒绝。

对应：

线程池七参数。

第十部分：日志设计（学习模式）

每一步打印：

traceId

线程名称

时间

库存

锁状态

耗时


例如：

============


秒杀开始


traceId:
8888


thread:
pool-1-thread-8


库存:

1000


执行:

Redis Lua


结果:

成功


库存:

999


耗时:

5ms


============


你可以观察：

不同并发下：

线程变化。

最终你会掌握：
知识点	在项目哪里体现
synchronized	单机库存扣减
volatile	库存状态
AQS	ReentrantLock分析
CAS	Redis锁思想
线程池	秒杀请求处理
Redis	库存热点
MySQL事务	订单一致性
Spring Cloud	微服务
限流	Gateway
最终一致	订单补偿
后续我会继续输出：

第二部分：《项目目录结构 + Docker Compose + Nacos配置 + MySQL初始化SQL + Spring Cloud模块设计》

然后：

第三部分：《从错误版本开始实现，逐步引入 synchronized → Redis锁 → Lua → MQ，让你通过代码理解Java并发》

这个项目完成后，你不仅能回答秒杀系统设计题，也能把它作为简历中的一个“高并发秒杀系统”项目。你上传的知识体系里，系统设计本身也是 P1 重要能力，其中明确包含“秒杀 / 限流 / 分布式ID”等能力点。

继续输出第二部分：《项目目录结构 + Docker Compose + Nacos配置 + MySQL初始化SQL + Spring Cloud模块设计》
双十一秒杀系统设计文档（第二部分）
项目目录结构 + Docker Compose + Nacos配置 + MySQL初始化SQL + Spring Cloud模块设计

版本：

Seckill System V1.0

目标：

在 Windows + Docker Desktop 环境运行

IDEA启动Spring Cloud微服务

ApiFox模拟高并发请求

逐步学习Java并发、Redis、MySQL事务、分布式锁
1. 项目整体目录设计

最终项目结构：

seckill-system
│
├── docker
│   │
│   ├── docker-compose.yml
│   │
│   ├── mysql
│   │   └── init.sql
│   │
│   ├── nacos
│   │
│   ├── redis
│   │
│   └── seata
│
│
├── seckill-common
│
│
├── seckill-gateway
│
│
├── seckill-stock
│
│
├── seckill-order
│
│
├── seckill-payment
│
│
├── seckill-service
│
│
├── pom.xml
│
└── README.md

2. 微服务职责设计
2.1 seckill-gateway

作用：

所有请求入口。

访问：

用户

 |

Gateway

 |

业务服务

负责：

路由
统一认证
限流
日志记录

技术：

Spring Cloud Gateway
Sentinel

端口：

9000
2.2 seckill-service（核心）

秒杀入口。

接口：

POST

/api/seckill/buy

职责：

协调：

库存服务

订单服务

支付服务

类似：

订单系统里的：

业务编排层。

端口：

8081
2.3 seckill-stock

库存服务。

负责：

商品库存。

接口：

扣减库存

恢复库存

查询库存

例如：

POST

/stock/decrease

端口：

8082
2.4 seckill-order

订单服务。

生成：

秒杀订单

数据：

订单号

用户

商品

金额

状态

端口：

8083
2.5 seckill-payment

支付服务。

模拟：

用户余额扣减。

接口：

/payment/pay

端口：

8084
3. Maven父工程设计

根目录：

pom.xml

作用：

统一管理版本。

示例：

<modules>

    <module>seckill-common</module>

    <module>seckill-gateway</module>

    <module>seckill-service</module>

    <module>seckill-stock</module>

    <module>seckill-order</module>

    <module>seckill-payment</module>

</modules>
4. 技术版本
Java
JDK17

原因：

Spring Boot 3最低要求。

Spring Boot
3.2.x
Spring Cloud
2023.x
Spring Cloud Alibaba
2023.x
MySQL
8.0
Redis
7.x
5. Docker环境设计

Windows：

Windows 11

 |

Docker Desktop

 |

Linux Container


启动组件：

组件	作用	端口
MySQL	业务数据库	3306
Redis	缓存库存	6379
Nacos	注册配置中心	8848
Seata	分布式事务	8091
6. docker-compose.yml

位置：

docker/docker-compose.yml


内容：

version: '3'


services:


 mysql:

  image: mysql:8.0

  container_name: seckill-mysql

  environment:

    MYSQL_ROOT_PASSWORD: 123456

    MYSQL_DATABASE: seckill


  ports:

    - "3306:3306"


  volumes:

    - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql



 redis:

  image: redis:7


  container_name:
    seckill-redis


  ports:

    - "6379:6379"



 nacos:


  image:
    nacos/nacos-server:v2.3.2


  container_name:
    seckill-nacos


  environment:


    MODE:
      standalone


    NACOS_AUTH_ENABLE:
      false



  ports:


    - "8848:8848"



 seata:


  image:
    seataio/seata-server:2.0.0


  container_name:
    seckill-seata



  ports:


    - "8091:8091"


启动：

进入docker目录：

docker compose up -d

查看：

docker ps

应该看到：

seckill-mysql

seckill-redis

seckill-nacos

seckill-seata

7. Nacos设计

访问：

http://localhost:8848/nacos

创建Namespace：

seckill-dev

服务列表：

启动后：

seckill-gateway

seckill-service

seckill-stock

seckill-order

seckill-payment


都会注册。

8. Nacos配置中心

创建配置：

seckill-service.yaml

内容：

server:

 port:
 8081



spring:

 datasource:

  url:

   jdbc:mysql://localhost:3306/seckill


  username:

   root


  password:

   123456



 redis:

  host:

   localhost


  port:

   6379



cloud:

 nacos:

  discovery:

   server-addr:

    localhost:8848

9. MySQL数据库设计

数据库：

seckill
9.1 商品库存表

表：

goods_stock

SQL：

CREATE TABLE goods_stock
(

id BIGINT PRIMARY KEY,


goods_name VARCHAR(100),


stock INT,


price DECIMAL(10,2),


version INT DEFAULT 0,


create_time DATETIME

);



初始化：

INSERT INTO goods_stock
VALUES
(
1,
'iPhone17 Pro',
1000,
7999,
0,
NOW()
);

9.2 秒杀订单表
CREATE TABLE seckill_order
(

id BIGINT PRIMARY KEY,


order_no VARCHAR(64),


user_id BIGINT,


goods_id BIGINT,


amount DECIMAL(10,2),


status VARCHAR(20),


create_time DATETIME

);

9.3 用户余额表

模拟支付。

CREATE TABLE user_account
(

id BIGINT PRIMARY KEY,


user_id BIGINT,


balance DECIMAL(10,2)

);


初始化：

INSERT INTO user_account
VALUES

(
1,
10001,
100000
);

9.4 并发执行日志表

重点：

用于学习。

CREATE TABLE concurrency_log
(

id BIGINT AUTO_INCREMENT PRIMARY KEY,


trace_id VARCHAR(64),


service_name VARCHAR(50),


thread_name VARCHAR(100),


step VARCHAR(100),


before_value VARCHAR(100),


after_value VARCHAR(100),


lock_type VARCHAR(50),


cost_time BIGINT,


create_time DATETIME

);


例如：

记录：

traceId:

abc123


thread:

http-nio-8081-exec-10


step:

redis扣库存


before:

1000


after:

999


lock:

lua


cost:

5ms

10. seckill-common模块设计

公共模块。

目录：

seckill-common

├── entity

├── dto

├── result

├── exception

└── utils

Result统一返回
@Data
public class Result<T>{


private Integer code;


private String msg;


private T data;


}

TraceId工具

每个请求：

生成唯一ID。

public class TraceUtil {


public static String getTraceId(){

 return UUID.randomUUID()
 .toString();

}


}


目的：

观察并发。

11. seckill-service业务流程

调用链：

ApiFox

 |

Gateway

 |

seckill-service


 |

 ----------------

 |              |

stock        order


 |

payment

12. Spring Cloud调用方式

使用：

OpenFeign

例如：

库存接口：

@FeignClient(
name="seckill-stock"
)
public interface StockFeign{


@PostMapping(
"/stock/decrease"
)

Result decrease(
Long goodsId
);


}

13. 为什么拆微服务？

不是为了复杂。

而是为了学习：

synchronized问题

单体：

一个JVM

一个锁


正常。

微服务：

服务A JVM

服务B JVM


锁失效。

Redis分布式锁

需要：

跨JVM同步。

Seata

学习：

分布式事务。

14. 本阶段完成后，你具备环境

启动：

docker compose up


↓

MySQL

Redis

Nacos

Seata


↓

IDEA启动：

gateway

service

stock

order

payment


↓

ApiFox调用

↓

观察日志

↓

学习并发问题

下一部分（第三部分）

下一章会进入真正核心：

《从错误实现开始理解Java并发》

会按照生产事故演进：

Version 0：

无锁扣库存

结果：

库存变负数

学习：

线程安全
临界区
数据竞争
Version 1：

加入 synchronized

发现：

单机解决

但是：

集群失效

学习：

synchronized
Monitor
JVM锁
Version 2：

ReentrantLock + AQS

学习：

AQS
state
CAS
队列
Version 3：

Redis分布式锁

学习：

分布式锁
CAP
锁续期
Version 4：

Redis Lua

学习：

原子性
最终一致
Version 5：

线程池 + MQ削峰

学习：

ThreadPoolExecutor
BlockingQueue
拒绝策略

这部分才是真正把你薄弱的 Java 并发和系统设计连接起来。你做完这个项目，面试中的“双十一秒杀系统设计”基本可以从架构、代码、底层原理三个层面回答。

继续输出第三部分：《从错误版本开始实现，逐步引入 synchronized → Redis锁 → Lua → MQ，让你通过代码理解Java并发》
双十一秒杀系统设计文档（第三部分）
从错误版本开始实现：无锁 → synchronized → Redis锁 → Lua → MQ
目标

这一部分不是为了快速写出一个“正确”的秒杀系统。

而是：

故意设计错误版本，然后通过并发压测观察问题，再引入新的技术解决问题。

这才是理解 Java 并发最有效的方法。

最终演进路线：

Version 0
普通代码扣库存
        |
        ↓
发现超卖

Version 1
synchronized
        |
        ↓
单机解决，集群失败

Version 2
ReentrantLock(AQS)
        |
        ↓
理解Java锁机制

Version 3
Redis分布式锁
        |
        ↓
跨JVM解决

Version 4
Redis Lua原子扣减
        |
        ↓
高性能库存控制

Version 5
MQ异步削峰
        |
        ↓
支撑50万QPS
一、Version 0：最原始实现（错误方案）
1.1 业务需求

用户请求：

购买商品
    |
检查库存
    |
库存-1
    |
创建订单

代码：

@PostMapping("/buy")
public Result buy(Long goodsId, Long userId){

    //查询库存
    GoodsStock stock =
        stockMapper.selectById(goodsId);


    if(stock.getStock() <= 0){

        return Result.fail("库存不足");

    }


    //扣库存
    stock.setStock(
        stock.getStock()-1
    );


    stockMapper.update(stock);


    //创建订单
    createOrder(userId,goodsId);


    return Result.success();

}
1.2 单线程没有问题

假设库存：

stock = 1

用户A：

查询

1

扣减

0

正常。

1.3 并发问题出现

两个线程：

库存=1


线程A              线程B


查询库存            查询库存

看到1              看到1


扣减                扣减


0                  0


创建订单            创建订单


最终：

两个订单成功

但是库存只有一个

这叫：

超卖
1.4 为什么发生？

因为：

检查库存：

if(stock>0)

和：

扣库存：

stock--

不是一个原子操作。

实际上：

读取

↓

计算

↓

写入

三个步骤。

对应Java并发知识

这里对应：

原子性问题

Java里面：

i++

也是一样。

实际上：

load

add

store

二、Version 1：加入 synchronized

我们想到：

多线程同时修改库存，那加锁。

代码：

public synchronized Result buy(
Long goodsId,
Long userId
){


    GoodsStock stock =
       stockMapper.selectById(goodsId);


    if(stock.getStock()<=0){

        return Result.fail();

    }


    stock.setStock(
       stock.getStock()-1
    );


    stockMapper.update(stock);


    createOrder();


    return Result.success();

}
2.1 发生什么？

现在：

线程A进入：

synchronized

       |
       |
   查询库存

       |
       |
   扣减

       |
       |
   释放锁


线程B等待。

结果：

库存：

1

↓

0

不会超卖。

2.2 这里学习 synchronized
synchronized锁什么？

这个方法：

public synchronized Result buy()

实际上：

锁：

this对象

等价：

synchronized(this){

}
2.3 问题：生产环境不能这样

现在部署：

seckill-service-1

        JVM


seckill-service-2

        JVM


两个用户：

请求A

进入服务1

获得锁


请求B

进入服务2

获得锁


为什么？

因为：

两个JVM：

锁对象不同


所以：

仍然超卖。

面试回答

如果问：

synchronized能解决秒杀库存问题吗？

回答：

单机环境可以。

但是微服务集群环境无法解决。

因为synchronized基于JVM Monitor，
锁只存在当前JVM。
三、Version 2：ReentrantLock + AQS

我们换：

private final Lock lock =
new ReentrantLock();


public Result buy(){


lock.lock();


try{


 //扣库存


}finally{


lock.unlock();


}

}

3.1 为什么不用synchronized？

因为：

Lock提供更多能力。

例如：

可尝试获取
boolean result =
lock.tryLock(
100,
TimeUnit.MILLISECONDS
);


意思：

等待100ms。

拿不到直接失败。

秒杀非常适合。

可中断
lock.lockInterruptibly();

3.2 这里理解AQS

ReentrantLock底层：

ReentrantLock

       |

       |

     AQS

       |

       |

state

       |

       |

CLH等待队列

获取锁过程

线程A：

CAS

state:

0 → 1


成功。

线程B：

CAS

0 → 1

失败


进入：

等待队列
对应面试问题

问：

ReentrantLock为什么线程安全？

回答：

底层通过AQS维护同步状态state，
利用CAS竞争锁，
失败线程进入等待队列。
但是：

依旧：

不能解决分布式问题

原因：

还是：

JVM锁

四、Version 3：Redis分布式锁

现在架构：

服务1

JVM


服务2

JVM


服务3

JVM


        |
        |

      Redis


所有服务：

竞争Redis。

4.1 加锁

Redis：

命令：

SET lock:goods:1 user001 NX EX 10

解释：

NX:

不存在才创建


EX:

10秒自动过期

Java代码
Boolean lock = 
redisTemplate.opsForValue()
.setIfAbsent(
"lock:"+goodsId,
userId,
10,
TimeUnit.SECONDS
);


if(!lock){

 return fail("抢购失败");

}

4.2 扣库存

拿锁成功：

Redis锁

↓

查询库存

↓

MySQL扣减

↓

创建订单

↓

释放锁

4.3 释放锁问题

错误：

redis.delete(lockKey);


问题：

线程A：

获得锁

10秒

业务：

执行15秒

锁过期。

线程B：

获得锁

此时：

线程A：

delete(lockKey)

把B的锁删除。

解决：

value校验。

加锁：

lockKey

value:

uuid

释放：

Lua：

如果value属于自己

才删除

五、Version 4：Redis Lua实现库存扣减

前面Redis锁：

性能还是有限。

因为：

10000用户：

都竞争锁。

秒杀库存：

其实只需要：

保证：

库存判断

+

库存减少


原子。

Lua脚本

Redis：

local stock =
redis.call(
'get',
KEYS[1]
)


if tonumber(stock)<=0 then

return -1

end


redis.call(
'decr',
KEYS[1]
)


return 1

Java调用
DefaultRedisScript<Long> script;


Long result =
redisTemplate.execute(
script,
Arrays.asList(
"seckill:stock:1"
)
);


if(result==1){

 //进入订单流程

}

为什么Lua可以？

Redis：

单线程执行命令。

Lua脚本：

执行期间：

不会插入其他命令。

所以：

判断库存

+

扣库存


成为一个整体。

这里对应：
volatile?

Redis Lua解决：

原子性。

不是volatile。

六、Version 5：MQ异步削峰

即使Lua：

也扛不住：

50万QPS。

为什么？

因为后面还有：

订单创建

支付扣款

数据库写入

引入MQ

架构：

用户请求


 |

Gateway


 |

Redis扣库存


 |

发送消息


 |

Kafka/RabbitMQ


 |

订单消费者


 |

MySQL
秒杀接口变轻

之前：

请求

↓

扣库存

↓

创建订单

↓

支付

↓

返回


现在：

请求

↓

Redis扣库存

↓

发送消息

↓

立即返回

消费者
@RabbitListener(
queues="seckill.order"
)
public void createOrder(
Message message
){


 //创建订单


}

七、完整生产级流程

最终：

用户

 |

Gateway

 |
限流

 |

seckill-service

 |

Redis Lua

 |
库存-1

 |

MQ

 |

order-service

 |

MySQL

 |

payment-service


八、对应Java并发知识地图
系统问题	Java知识
两个线程同时扣库存	线程安全
i++问题	原子性
单机锁	synchronized
Lock实现	AQS
CAS竞争	Unsafe/CAS
线程等待	阻塞队列
大量请求	线程池
异步处理	MQ
库存一致	事务
九、你的学习重点（结合这个项目）

你的薄弱点：

Java并发 30%

建议重点观察：

第一次压测

无锁版本：

看：

库存负数
订单数量
线程竞争
第二次

synchronized：

看：

吞吐下降

但是正确

理解：

锁粒度。

第三次

Redis Lock：

看：

多实例正确

性能提升
第四次

Lua：

看：

QPS提升

没有锁等待
第五次

MQ：

看：

系统抗峰值能力

下一部分建议进入：

《第四部分：代码实现篇》

内容：

Spring Cloud项目初始化
Gateway代码
秒杀Controller
四个版本库存扣减代码
ApiFox并发测试方案
MySQL日志记录
如何观察线程池、锁竞争、QPS变化

这一部分会真正开始写代码，并且每个代码块都会标注：

【学习知识点】
【为什么这样写】
【生产问题】
【面试回答】

这样这个项目会变成你的 Java 并发实验场。